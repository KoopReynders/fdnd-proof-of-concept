<%- include('partials/header', {head:'', title:'Mijn sites'}); -%>

</head>
    <body>
        <main>
            <ul>
                <% sites.forEach(site => { %>
                    <li><a href="/<%= site.title %>"><%= site.title %></a></li>
                <% }) %>
            </ul>
        </main>
    </body>
</html>
